
#import Control
#import camera
# todo finish the control library, pull the final version down then import it like a file

def main():
    # control = Control()
    # cam = Camera()
    # control.calibrate()
    # move to offsets for machine x,y
    # while (control.y_currentPos <= control.endOfY): # range tolerance? range(control.endOfY, control.endOfY + 5)
    #     while (control.x_currentPos <= control.endOfX):
    #         cam.startRecording(5, filename)
    #         control.moveToNextChip
    #     control.moveUpRow()
    pass

main()